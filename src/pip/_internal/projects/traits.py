"""Names that can be used for project traits.

These are specifically named so that typos are caught at the time the classes
are evaluated (since traits are declared in the class body).
"""

archive = 'archive'
directory = 'directory'
editable = 'editable'
local = 'local'
named = 'named'
remote = 'remote'
unnamed = 'unnamed'
vcs = 'vcs'
wheel = 'wheel'
